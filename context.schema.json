{
  "$schema": "https://raw.githubusercontent.com/AkaraChen/ctxl/main/core/schema/loader/ctxl.schema.json",
  "name": "hnm",
  "description": "Agent documentation harness: root AGENTS.md, spec, PRD/ADR collections, and workflow skills.",
  "generation": {
    "mode": "existing-module",
    "output": "internal/generated",
    "ctxl_version": "v0.0.0-20260907170641-0f64a6c4689a",
    "package": "generated"
  },
  "skills": [
    {
      "type": "builtin",
      "description": "Operate the hnm documentation harness through the hnm CLI instead of editing harness files by hand.",
      "allowed-tools": "Bash(hnm:*)"
    },
    {
      "type": "custom",
      "directory": ".agents/skills/feature-dev"
    },
    {
      "type": "custom",
      "directory": ".agents/skills/git-commit"
    }
  ],
  "entities": [
    {
      "name": "agents",
      "kind": "singular",
      "format": "markdown",
      "path": "AGENTS.md",
      "location": "root",
      "scope": "project",
      "description": "Root agent instructions: project workflows, commands, and code style.",
      "body": "# Project workflows\n\n## Feature development\n\nFor every new feature, read `hnm skills get feature-dev` and use `$feature-dev` before implementation. Inspect the code and all project documentation, complete the one-question-at-a-time product and technical 质问, and record the agreed product requirements, architecture decisions, and general specification under `docs/` before changing feature code.\n\nDocumentation layout:\n\n| Path | Role |\n|------|------|\n| `docs/prd/` | Product requirements: problem, users, goals/non-goals, flows, acceptance criteria |\n| `docs/adr/` | Architecture decisions: context, choice, alternatives, trade-offs, failure bounds |\n| `docs/spec.md` | Single source of truth for terminology, observable contracts, and system-wide invariants |\n\nRead the relevant docs before changing feature code. Existing decisions live in `docs/adr/`, requirements in `docs/prd/`. Prefer updating those files over inventing parallel notes.\n\n## Documentation rules\n\n- Every completed feature 质问 leaves a PRD, at least one ADR when a material technical choice exists, and an update to `docs/spec.md` for any stable observable rule.\n- `docs/spec.md` stays implementation-independent: shared terminology, externally observable contracts, and invariants. Put feature rationale in the PRD and decision rationale in ADRs.\n- Use concise kebab-case filenames derived from the feature or decision (for example `docs/prd/cli-run.md`, `docs/adr/error-types.md`).\n- Prefer testable product statements in PRDs. Do not hide unresolved product questions inside implementation TODOs.\n- If code and docs disagree, surface the conflict during 质问 or before commit; do not silently pick one side.\n\n## Commit and ship\n\nBefore any commit, review staged, unstaged, and untracked changes against `docs/spec.md` and every relevant document under `docs/adr/` and `docs/prd/`. If the implementation changes a documented fact, update and stage the documentation in the same change. Read `hnm skills get git-commit` and use `$git-commit` to stage logical groups and create conventional commits from the diff.\n\n# Commands\n\nUse the project's existing build, test, and format commands.\n\n# Code style\n\n- Prefer small modules with clear ownership boundaries over large catch-all files.\n- Domain types and invariants belong in library code when the project grows past a single entry file.\n- Errors should be typed where boundaries matter; avoid stringly-typed failure modes for contracts covered by the spec.\n- Tests protect invariants and acceptance criteria from PRDs/spec, not implementation trivia.\n\n# Note\n\n`CLAUDE.md` is a symlink to this file — edit `AGENTS.md`.\n\nThis harness was installed by `hnm init`.\n"
    },
    {
      "name": "claude",
      "kind": "singular",
      "format": "symlink",
      "path": "CLAUDE.md",
      "target": "AGENTS.md",
      "location": "root",
      "scope": "project",
      "description": "Claude-facing alias; always a symlink to AGENTS.md so there is one source of truth."
    },
    {
      "name": "spec",
      "kind": "singular",
      "format": "markdown",
      "path": "docs/spec.md",
      "location": "root",
      "scope": "project",
      "description": "Single source of truth for terminology, observable contracts, and system-wide invariants.",
      "body": "# Specification\n\n## Product scope\n\nThis project is a greenfield or existing project that uses the agent documentation harness installed by `hnm`. Product purpose, users, and surface area beyond that harness should be refined through `$feature-dev` 质问 and recorded in `docs/prd/`, `docs/adr/`, and this file.\n\nOut of scope until explicitly specified: anything not yet accepted in a PRD.\n\n## Terminology\n\n- **Feature 质问**: the mandatory product-then-technical clarification loop driven by `$feature-dev` before implementation.\n- **PRD**: a product requirements document under `docs/prd/` describing problem, users, goals, non-goals, flows, failure behavior, and acceptance criteria.\n- **ADR**: an architecture decision record under `docs/adr/` capturing one material technical choice, alternatives, and consequences.\n- **Spec**: this file — the single source of truth for shared terminology, observable contracts, and system-wide invariants.\n\n## Observable contracts\n\n### Documentation harness\n\n- New product behavior is defined in `docs/prd/` before feature code lands.\n- Material technical choices are recorded in `docs/adr/` before or with the code that depends on them.\n- Stable, implementation-independent rules merge into this `docs/spec.md`.\n- Feature work that changes terminology, contracts, invariants, or failure behavior updates this file in the same change set.\n- Agents must not implement feature code during 质问; the accepted PRD/ADR/spec set is the source of truth for implementation.\n\n## System-wide constraints\n\n- Repository agent entrypoint is root `AGENTS.md` (`CLAUDE.md` is a symlink to it).\n- Read workflow skills with `hnm skills get feature-dev` and `hnm skills get git-commit`; use `hnm skills path NAME` for their supporting files.\n- Commit attempts should re-check the working tree against this specification and relevant PRDs/ADRs before landing.\n\n## Current implementation status\n\n- Documentation harness directories and agent workflow files were installed by `hnm init`.\n- Product features beyond the harness are not specified yet — run `$feature-dev` for the next feature.\n"
    },
    {
      "name": "prd",
      "kind": "plural",
      "format": "markdown",
      "path": "docs/prd",
      "location": "root",
      "scope": "project",
      "description": "Product requirements, one kebab-case file per feature: problem, users, goals, flows, acceptance criteria."
    },
    {
      "name": "adr",
      "kind": "plural",
      "format": "markdown",
      "path": "docs/adr",
      "location": "root",
      "scope": "project",
      "fields": [
        {
          "name": "status",
          "type": "string",
          "description": "Proposed, Accepted, or Superseded"
        }
      ],
      "description": "Architecture decisions, one kebab-case file per decision: context, choice, alternatives, trade-offs."
    }
  ]
}
